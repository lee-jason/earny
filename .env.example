# Vercel Postgres (auto-set by Vercel when you add Postgres storage)
POSTGRES_PRISMA_URL="postgresql://user:password@localhost:5432/earny?schema=public"
POSTGRES_URL_NON_POOLING="postgresql://user:password@localhost:5432/earny?schema=public"

# NextAuth - generate secret with: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=""

# Google OAuth (get from Google Cloud Console: https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Production database URL (for running migrations against prod)
POSTGRES_PRISMA_URL_PRODUCTION=""
